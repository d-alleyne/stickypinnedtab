{
	"manifest_version": 2,
	"name": "Sticky Pinned Tab",
	"description": "Moving a pinned tab between browser windows keeps it pinned, just like Chromium-based browsers",
	"version": "0.0.4",
	"icons": {
		"64": "icons/icon.png"
	},
	"background": {
		"scripts": [
			"background_script.js"
		]
	}
}